@inherits FieldBase<string, AddressFieldModel, NotEmptyValidator, MudTextField<string>>

<MudTextField Class="@Class"
              Style="@Style"
              Label="@Label"
              Value="Model.Value"
              ValueChanged="async (string input)=> await Changed(input.Trim())"
              Validation="_Validator.ValidateValue"
              For="@(() => Model.Value)"
              Variant="Variant"
              Disabled="Disabled"
              Immediate="Immediate"
              @ref="Field" />