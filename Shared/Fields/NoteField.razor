@inherits FieldBase<string, NoteFieldModel, BypassValidator, MudTextField<string>>

<MudTextField Label="@Label"
              Value="Model.Value"
              ValueChanged="async (string input)=> await Changed(input.Trim())"
              Validation="_Validator.ValidateValue"
              For="@(() => Model.Value)"
              Variant="Variant"
              Disabled="Disabled"
              Immediate="Immediate"
              Lines="8"
              Class="@Class"
              Style="@Style"
              @ref="Field" />