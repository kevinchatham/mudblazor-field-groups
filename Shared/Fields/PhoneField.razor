@inherits FieldBase<string, PhoneFieldModel, PhoneValidator, MudTextField<string>>

<MudTextField Label="@Label"
              Value="Model.Value"
              ValueChanged="async (string input)=> await Changed(input.Trim())"
              For="@(() => Model.Value)"
              Validation="_Validator.ValidateValue"
              Variant="Variant"
              Disabled="Disabled"
              Immediate="Immediate"
              Class="@Class"
              Style="@Style"
              @ref="Field" />