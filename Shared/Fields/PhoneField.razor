@inherits FieldBase<string, PhoneFieldModel, MudTextField<string>>

<MudTextField Label="@Label"
              Value="Model.Value"
              ValueChanged="async (string input)=> await Changed(input.Trim())"
              For="@(() => Model.Value)"
              Validation="Validator.Validate"
              Variant="Variant"
              Disabled="Disabled"
              Immediate="Immediate"
              Class="@Class"
              Style="@Style"
              @ref="Field" />